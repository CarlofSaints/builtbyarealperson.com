# ─────────────────────────────────────────────────────────────────────────────
# Copy to .env.local for local work, and set the same keys in Vercel
# (Production AND Preview. A key set only in Production makes previews lie).
# ─────────────────────────────────────────────────────────────────────────────

# Resend. Required for any email to go out at all.
RESEND_API_KEY=

# The From address. The domain here MUST be the exact domain verified in Resend,
# or every send is rejected.
EMAIL_FROM="Built By A Real Person <hello@builtbyarealperson.com>"
EMAIL_REPLY_TO=hello@builtbyarealperson.com

# Where new leads are emailed to. Without this you never hear about a submission.
NOTIFY_EMAIL=

# Vercel Blob. Lead records are stored here, one PRIVATE file per lead. The
# store itself must be created in private mode, which cannot be changed later.
# Connecting the store to the project normally sets BLOB_STORE_ID and uses OIDC,
# in which case this static token is not needed. Either one is enough.
BLOB_READ_WRITE_TOKEN=

# Protects /api/cron/followup. The route refuses to run without it rather than
# running unauthenticated, so this is not optional.
CRON_SECRET=

# How long after a submission the booking email goes out. Default 120 minutes.
FOLLOWUP_DELAY_MINUTES=120

# Microsoft Bookings or Calendly link for the 30-minute call. If left blank the
# booking email falls back to asking the customer to reply with times.
NEXT_PUBLIC_BOOKING_URL=

# Optional. Leave blank and no phone number is shown anywhere on the site.
NEXT_PUBLIC_PHONE=

# The address shown on the site and used as the reply-to. Point this at an
# address that ACTUALLY RECEIVES mail. Resend can send *as* an address with no
# mailbox behind it, but a reply to one bounces, and the bounce goes to the
# customer, not to you. Defaults to hello@builtbyarealperson.com.
NEXT_PUBLIC_CONTACT_EMAIL=

# ─────────────────────────────────────────────────────────────────────────────
# Back office (/admin). Without BOTH of these nobody can sign in. The login
# page says which one is missing rather than just refusing.
# ─────────────────────────────────────────────────────────────────────────────

# The password for /admin. Minimum 12 characters, enforced. This is the only
# thing standing between the internet and every customer's contact details.
ADMIN_PASSWORD=

# Signs the admin session cookie. A long random string. `Openssl rand -base64 32`.
# Changing it, or changing ADMIN_PASSWORD, signs every existing session out.
ADMIN_SESSION_SECRET=

# Resend delivery webhook. The signing secret Resend shows when you add the
# endpoint (starts with whsec_). Without it /api/webhooks/resend returns 503
# rather than accepting unsigned events. An open endpoint that writes to lead
# records is an endpoint anyone can write to.
RESEND_WEBHOOK_SECRET=
